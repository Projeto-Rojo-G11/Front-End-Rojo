# import requests
# import json

# def connect(key, base_url):
#     headers = { "Authorization" : "Bearer {key}",
#         "Accept" : "application/json",
#         "Content-Type" : "application/json"
#     }
#     base_url = base_url

#     return (headers, base_url)

# conexao = connect()

# # teste de verificacao
# resp = requests.get(url=conexao.get['base_url'] + "api/dashboards/home", verify=False, headers=conexao.get['headers'])
# data = resp.json()


